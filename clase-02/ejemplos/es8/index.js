//===============================================================//
// ES8 incluyo metodos para objetos:
// Object.entries
// Object.keys
// Object.values
// Tambien Incluyo async/await "LO VEREMOS LA SIGUIENTE CLASE"
//============================================================//

/* testObjectEntries = () => {
    const car = {brand: "KIA", model:"MORNING", amount: 9990, currency: "USD"}
    const finalResult = Object.entries(car)
    console.log('[testObjectEntries] ->', finalResult)
} */

/* testObjectKeys = () => {
    const car = {brand: "KIA", model:"MORNING", amount: 9990, currency: "USD"}
    const finalResult = Object.keys(car)
    console.log('[testObjectKeys] ->', finalResult)
} */

/* testObjectValues = () => {
    const car = {brand: "KIA", model:"MORNING", amount: 9990, currency: "USD"}
    const finalResult = Object.values(car)
    console.log('[testObjectValues] ->', finalResult)
} */